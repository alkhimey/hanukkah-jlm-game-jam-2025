extends Node

# poisson_rate average number of arrivals per second. Lower is faster.
const client_arrival_rate = 2.0
 
# Points per second per client.
const lose_rate = 1 

const max_clients_in_queue = 10
